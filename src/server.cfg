//server.cfg is loaded before any other script during server
//initialization and acts as the engine's configuration file

void SetupServer(ServerConfig@ config)
{
	config.SetDatabasePathAndFile("./game/database/", "test.db");
	config.SetGameScriptPath("./game/scripts/");
	config.SetGameBindAddress("127.0.0.1", 9001);
}